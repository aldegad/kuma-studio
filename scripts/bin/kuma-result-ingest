#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
# shellcheck source=/dev/null
source "$SCRIPT_DIR/kuma-cli-common.sh"

ingested_count="$(ingest_missing_result_files_to_vault)"
printf '%s건 ingest됨\n' "$ingested_count"
