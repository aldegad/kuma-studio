interface:
  display_name: "Agent Picker"
  short_description: "Read picks, screenshots, and shared notes"
  default_prompt: "Use $agent-picker to read the latest selection, update the shared note, and work from the picked UI context."
